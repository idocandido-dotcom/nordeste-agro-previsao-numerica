name: Workflow antigo desativado

on:
  workflow_dispatch:

jobs:
  desativado:
    runs-on: ubuntu-latest
    steps:
      - name: Desativado
        run: echo "Workflow antigo desativado. Usar atualizar-clima-matopiba.yml"
